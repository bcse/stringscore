============
Installation
============

At the command line::

    $ easy_install stringscore

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv stringscore
    $ pip install stringscore